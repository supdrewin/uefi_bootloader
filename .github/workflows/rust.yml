name: Rust CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  cargo:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run `cargo check`
      run: cargo check
    - name: Run `cargo build`
      run: cargo build
